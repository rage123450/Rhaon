[ 할 거 ]
클래스로 만들기 ㅇㅇ
- 그냥 클래스 만들고 new하고 함수 호출하면 됨.

클릭하면 숫자 올라가기 ㅇㅇ
- 컨트롤 추가해서 숫자 쌩으로 출력
- 변수로 출력시키기
- 변수들을 컨트롤 할려면 지역변수를 멤버변수로 바꿔야함.
- 업데이트 역활을 하는 함수를 호출해야 함.
- 일단 그냥 숫자 올라가도록 함.
- onTimer가 1초에 한번씩 호출되는 듯?
- bool CTRDemoStartUI::onButtonClick(CGUIControlID ctrlID, CCommandID cmdID)에서
  해당 클래스 onButtonClick를 호출함. 근데 왜 되는 거지?
  여러개 이미지버튼을 만들면 어떤걸 클릭해도 숫자가 올라간다.
  어떤걸 클릭하는지 알 수 있다. CGUIControlID ctrlID

키보드로 이미지 이동시키기 ㅇㅇ

키보드로 입력받기 ㅇㅇ



[ 한 거 ]
gui 툴 위치 : 찾아서 고정해둠.
gui 띄웠다.
인트라넷 계정, 연차 계정 생성 및 확인 완료
클래스로 만들었음
숫자 출력 시킴.
GUI이벤트 처리 클래스는 싱글톤 구현이 기본인지? 아니면 상황에 따라 다른지?



[ 안 거 ]
인크리드 빌드란?
-  VC++ 의 느린(?) 컴파일 속도를 개선하기 만들어진 네트워크 분산 빌딩시스템

컴포지션(합성) 관계란?
- 전체와 부분
- 다이아를 가진 쪽이 전체
- 전체가 부분의 수명을 책임진다.(전체가 부분을 생성, 같이 소멸, 깊은 복사)

디펜덴시(의존) 관계란?
- 대상 클래스의 객체 생성, 객체 사용, 메서드 호출, 객체 리턴, 매개변수로 해당 객체를 받는 것 등등
- 해당 객체의 참조를 계속 유지하지 않음.

어소시에이션(연관) 관계란?
- 의존관계랑 유사하나 참조를 유지함.

초기화 시점
- C++11 도입으로 선언과 동시에 초기화 가능
- 선언 동시 초기화와 멤버 이니셜라이징이 같이 있다면 멤버 이니셜라이징이 우선시 된다.

static_cast
- 컴파일 타임에 형변환에 대한 타입 오류를 잡아준다.
- 상속 관계에 있는 포인터 다운 캐스팅 가능(안전하지 않음)


















