#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

auto main()->int
{
	vector<int> vec = { 1,2,3,4,5 };

	//vec.erase(remove(begin(vec), end(vec),), end(vec));

	return 0;
}